import { ArrowLeft, CalendarDays, ClipboardList, LogIn, ShieldCheck, UserRound } from "lucide-react";
import { Link } from "wouter";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";
import { useAuth } from "@/_core/hooks/useAuth";

function formatDate(value: Date | string) {
  return new Intl.DateTimeFormat("en-US", { dateStyle: "medium" }).format(new Date(value));
}

function formatPrice(priceCents: number) {
  return new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" }).format(priceCents / 100);
}

export default function Account() {
  const { user, isAuthenticated, loading: authLoading, logout } = useAuth();
  const accountQuery = trpc.account.me.useQuery(undefined, { enabled: isAuthenticated, retry: false });
  const ordersQuery = trpc.account.orders.useQuery(undefined, { enabled: isAuthenticated, retry: false });

  if (authLoading) {
    return <div className="grid min-h-screen place-items-center bg-[#f4f7f8] text-sm text-[#6e838a]">Checking your session…</div>;
  }

  if (!isAuthenticated || !user) {
    return <div className="min-h-screen bg-[#f4f7f8] text-[#102b3a]"><header className="border-b border-[#dce7e8] bg-white/70"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5 lg:px-10"><Link href="/" className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-2xl bg-[#0b3444] text-[#b8e5d3]"><ShieldCheck size={21} /></span><span><span className="block text-[11px] font-semibold uppercase tracking-[0.24em] text-[#178b78]">Security lab</span><span className="block text-xl font-semibold tracking-tight">VulnMart</span></span></Link><Link href="/" className="text-sm font-semibold text-[#55707a]">Back to catalogue</Link></div></header><main className="mx-auto flex min-h-[calc(100vh-81px)] max-w-xl items-center justify-center px-6 py-16"><section className="w-full rounded-[2rem] border border-[#dce7e8] bg-white p-8 text-center shadow-[0_20px_60px_rgba(16,43,58,0.07)] sm:p-12"><span className="mx-auto grid size-14 place-items-center rounded-2xl bg-[#eaf7f1] text-[#17755f]"><UserRound size={25} /></span><p className="mt-7 text-xs font-semibold uppercase tracking-[0.2em] text-[#178b78]">Protected area</p><h1 className="mt-3 text-3xl font-semibold tracking-[-0.03em]">Sign in to view your account</h1><p className="mx-auto mt-4 max-w-sm leading-7 text-[#6e838a]">Account and order data are protected by the session identity. The browser is never treated as proof of ownership.</p><button type="button" onClick={() => startLogin()} className="mt-8 inline-flex items-center gap-2 rounded-full bg-[#0b3444] px-5 py-3.5 text-sm font-semibold text-white hover:bg-[#16485b]"><LogIn size={17} /> Sign in securely</button><Link href="/" className="mt-5 block text-sm font-semibold text-[#17755f]">Continue as a public visitor <span aria-hidden="true">→</span></Link></section></main></div>;
  }

  return <div className="min-h-screen bg-[#f4f7f8] text-[#102b3a]"><header className="border-b border-[#dce7e8] bg-white/70"><div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-5 lg:px-10"><Link href="/" className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-2xl bg-[#0b3444] text-[#b8e5d3]"><ShieldCheck size={21} /></span><span><span className="block text-[11px] font-semibold uppercase tracking-[0.24em] text-[#178b78]">Security lab</span><span className="block text-xl font-semibold tracking-tight">VulnMart</span></span></Link><div className="flex items-center gap-4"><span className="hidden text-sm text-[#55707a] sm:block">{accountQuery.data?.name ?? user.name ?? user.email ?? "Signed-in learner"}</span><button type="button" onClick={() => logout()} className="text-sm font-semibold text-[#55707a] hover:text-[#102b3a]">Sign out</button></div></div></header><main className="mx-auto max-w-7xl px-6 py-12 lg:px-10 lg:py-16"><Link href="/" className="inline-flex items-center gap-2 text-sm font-semibold text-[#17755f]"><ArrowLeft size={16} /> Back to catalogue</Link><div className="mt-8 flex flex-col justify-between gap-5 border-b border-[#dce7e8] pb-8 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-[#178b78]">Protected workspace</p><h1 className="mt-3 text-4xl font-semibold tracking-[-0.04em] sm:text-5xl">Your account</h1><p className="mt-3 text-[#6e838a]">A user-scoped view of your VulnMart learning activity.</p></div><span className="inline-flex items-center gap-2 self-start rounded-full border border-[#cfe3dc] bg-[#eaf7f1] px-3.5 py-2 text-xs font-semibold text-[#17755f] sm:self-auto"><span className="size-1.5 rounded-full bg-[#22a579]" /> Identity verified</span></div><div className="mt-10 grid gap-5 lg:grid-cols-[0.8fr_1.2fr]"><section className="rounded-3xl bg-[#0b3444] p-7 text-white"><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-2xl bg-white/10 text-[#b8e5d3]"><UserRound size={20} /></span><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[#b8e5d3]">Account identity</p><p className="mt-1 text-lg font-semibold">{accountQuery.data?.name ?? user.name ?? "VulnMart learner"}</p></div></div>{accountQuery.isLoading ? <div className="mt-10 h-16 animate-pulse rounded-2xl bg-white/10" /> : accountQuery.isError ? <p className="mt-10 text-sm leading-6 text-[#f1c4b5]" role="alert">Your account summary could not be loaded. The session is still active, but the profile endpoint needs attention.</p> : <div className="mt-10 space-y-4 text-sm text-[#c5d8d8]"><div className="flex items-center gap-3"><CalendarDays size={16} className="text-[#b8e5d3]" /> Member since {accountQuery.data?.memberSince ? formatDate(accountQuery.data.memberSince) : "—"}</div><div className="flex items-center gap-3"><ShieldCheck size={16} className="text-[#b8e5d3]" /> Role: {accountQuery.data?.role ?? user.role}</div><div className="border-t border-white/15 pt-4 text-[#86a6a5]">Email: {accountQuery.data?.email ?? user.email ?? "Not provided"}</div></div>}</section><section className="rounded-3xl border border-[#dce7e8] bg-white p-7"><div className="flex items-center justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[#178b78]">Order history</p><h2 className="mt-2 text-2xl font-semibold tracking-[-0.03em]">Your orders</h2></div><span className="grid size-11 place-items-center rounded-2xl bg-[#eaf7f1] text-[#17755f]"><ClipboardList size={20} /></span></div>{ordersQuery.isLoading ? <div className="mt-8 space-y-3"><div className="h-16 animate-pulse rounded-2xl bg-[#f1f5f5]" /><div className="h-16 animate-pulse rounded-2xl bg-[#f1f5f5]" /></div> : ordersQuery.isError ? <p className="mt-8 rounded-2xl border border-[#f1d4c9] bg-[#fff7f3] px-4 py-4 text-sm leading-6 text-[#9a503d]" role="alert">Your order history could not be loaded. No order data has been exposed to this page.</p> : ordersQuery.data?.length === 0 ? <div className="mt-8 rounded-2xl border border-dashed border-[#cbdcdb] bg-[#fbfdfc] px-5 py-10 text-center"><p className="font-semibold text-[#102b3a]">No orders yet</p><p className="mt-2 text-sm leading-6 text-[#6e838a]">Checkout is intentionally out of scope for Week 4. This empty state confirms that a signed-in user can see only their own order history.</p></div> : <div className="mt-8 space-y-3">{ordersQuery.data?.map(order => <div key={order.id} className="flex flex-col gap-3 rounded-2xl border border-[#e6eeee] bg-[#fbfdfc] p-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-semibold text-[#102b3a]">Order #{String(order.id).padStart(4, "0")}</p><p className="mt-1 text-xs text-[#819399]">{formatDate(order.createdAt)} · {order.status}</p></div><span className="font-semibold text-[#102b3a]">{formatPrice(order.totalCents)}</span></div>)}</div>}</section></div><p className="mt-8 text-xs leading-5 text-[#91a1a5]">Security boundary: order history is queried with the authenticated user ID on the server. The client cannot choose another user ID for this view.</p></main></div>;
}
